{
  "name": "fiber-pay-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AI Agent Payment SDK for CKB Lightning Network (Fiber Network) - Monorepo",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter='!@fiber-pay/cli' build && pnpm --filter='@fiber-pay/cli' build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf dist",
    "lint": "biome check packages/sdk/src packages/cli/src packages/node/src packages/agent/src",
    "lint:fix": "biome check --write packages/sdk/src packages/cli/src packages/node/src packages/agent/src",
    "format": "biome format --write packages/sdk/src packages/cli/src packages/node/src packages/agent/src",
    "format:check": "biome format packages/sdk/src packages/cli/src packages/node/src packages/agent/src",
    "e2e:testnet": "node scripts/e2e-testnet-dual-node.mjs",
    "prepare": "husky"
  },
  "keywords": [
    "ckb",
    "lightning",
    "fiber",
    "payment",
    "ai-agent",
    "mcp",
    "cryptocurrency",
    "nervos",
    "monorepo"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/node": "^25.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "lint-staged": {
    "packages/{sdk,cli,node,agent}/src/**/*.{ts,js}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  }
}
