---
"@fiber-pay/cli": patch
"@fiber-pay/runtime": patch
---

Fix lint warnings in runtime command fallback checks and restore runtime DTS build compatibility by avoiding optional-chain return type widening in proxy job hooks.
